# CLI reference

This page provides documentation for our command line tools.

Run `docling --help` or `docling-tools --help` after installing the project with `uv sync` (or `pip install docling`). For full CLI reference generation in this doc site, build with the complete project environment.
